<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :name %>
  <%= text_input f, :name %>
  <%= error_tag f, :name %>

  <%= label f, :phone %>
  <%= text_input f, :phone %>
  <%= error_tag f, :phone %>

  <%= label f, :message_subject %>
  <%= text_input f, :message_subject %>
  <%= error_tag f, :message_subject %>

  <%= label f, :message_text %>
  <%= text_input f, :message_text %>
  <%= error_tag f, :message_text %>

  <div>
    <%= submit "Save" %>
  </div>
<% end %>
